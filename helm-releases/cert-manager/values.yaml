# cert-manager Helm chart overrides
# Chart: jetstack/cert-manager
installCRDs: true
replicaCount: 1
resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi
prometheus:
  enabled: true
  servicemonitor:
    enabled: true
